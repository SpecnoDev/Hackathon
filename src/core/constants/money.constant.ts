/** PRD p.21: 15% keeps the demo number clean (R600 → "You will receive R510"). Fee model is still an open PO question. */
export const PLATFORM_FEE_BPS = 1500;
export const BPS_DENOMINATOR = 10_000;
export const CENTS_PER_RAND = 100;
