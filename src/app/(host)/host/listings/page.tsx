import { HostPlaceholder } from '@/core/layout';

// TODO(Marlon): the offerings list, the voice recorder and the publish flow live here.
export default function HostListingsPage() {
  return (
    <HostPlaceholder
      title="Listings"
      note="Recording an offering, editing it and publishing it happen on this tab."
    />
  );
}
