Screenshots the deck shows in a phone frame when it cannot run the thing live.

- `whatsapp-onboarding.png` — the WhatsApp onboarding conversation. Portrait, 390x844 or any 9:19.5 crop; it is displayed cover-aligned to the top.

Everything else on the feature slides is the running app in an iframe, so it never goes stale.
